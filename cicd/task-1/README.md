# Task 1 — Terraform CI placeholder

This directory contains a minimal, safe Terraform configuration used only for
CI validation in the portfolio. It contains no real resources or providers and
is intended to let `terraform init -backend=false` and `terraform validate`
run successfully during automated checks.

Replace these files with your real Terraform configuration when you're ready.
