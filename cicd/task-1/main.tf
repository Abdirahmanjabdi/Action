// Minimal Terraform configuration for CI validation (safe placeholder)
terraform {
  required_version = ">= 1.0"
}

locals {
  portfolio_placeholder = "This is a safe placeholder config for CI validation"
}
